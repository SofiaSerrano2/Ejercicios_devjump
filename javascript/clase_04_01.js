// Ejercicio 1 Crear la funcion: dividir
//  que pida 2 numeros , los divida y arroje un alerta con el resultado


//Ejercicio 2 crear la funcion: saludar.
// que pida un nombre y devuelva un alerta con el saludo al usuario/a (`hola ${nombre}`)


// Ejercicio 3
/* crear la funcion: tengoQueTrabajar 
que pida un nombre y un dia de la semana y evalue la siguiente condicion:
si la persona escribe sabado o domingo, le indique "no tenes que trabajar"
si escribe cualquier otro dia , le indique  ${nombre} tenes que ir a trabajar











function saludar(nombre) {
 console.log(`hola ${nombre}`);
}
saludar("pepe"); */


/* function sumarDosNumeros(num1, num2) {
    let suma = num1 + num2;
   console.log (suma);
  }
  sumarDosNumeros(1, 5, 7); */



/* function dividirDosNumeros(num1, num2) {
    let producto = num1 / num2;
    return producto;
  }
  dividirDosNumeros(6, 3); // 2
  console.log(producto); 
 */



/* function restar (num1,num2){
    let restar = num1 - num2;
    return restar;
}

let diferenciaDeResta = restar(10, 9);
console.log(diferenciaDeResta); // 1 */




/* Ejercicio crear funciones que resten , dividan, multipliquen 2 numeros */

/* 
 function suma(x, y) {
    // "x" e "y" son números
    // Suma "x" e "y" juntos y devuelve el valor
    // Tu código:
    return x + y;
   
  } */


/*   function tienenMismaLongitud(string1,string2) {
    // Devuelve "true" si las dos strings tienen la misma longitud
    // De lo contrario, devuelve "false"
    // Tu código:
  }
tienenMismaLongitud("hola", "r")

/*   function menosQueNoventa(num) {
    // Devuelve "true" si el argumento de la función "num" es menor que noventa
    // De lo contrario, devuelve "false"
    // Tu código:

    }
  } */
  
/*   function obtenerSaludo(nombre) {
    // Toma la string "nombre" y concatena otras string en la cadena para que tome la siguiente forma:
    // "Martin" -> "Hola Martin!"
    // Tu código:
    return 'Hola ' + nombre + '!';

  } */
  
